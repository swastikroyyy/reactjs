import React from 'react'

function Stu({ stu }) {
    return (
        <div>
            <h4>
                i am {stu.name}. my roll no is {stu.roll}.stream is {stu.stream}
            </h4>

        </div>
    )
}

export default Stu
